import React from 'react';

const Popup = () => {
  return (
    <dialog>
      <div>
        <h2>This is a popup</h2>
        <button onClick={() => {}}>Close</button>
      </div>
    </dialog>
  );
};

export default Popup;